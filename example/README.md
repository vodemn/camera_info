# camera_info example

Demonstrates how to use the `camera_info` plugin.

Displays per-camera optical metadata (focal length, aperture, field of view, zoom range, exposure offset step size) for every camera on the device.
